version: "3"
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 'book'

    volumes:
      #- src:/home/deploy/book/
      - ./demo/:/book/

    ports:
      - "3001:3001"

    restart: always

    command: ["mdbook", "serve", "-n", "0.0.0.0", "-p", "3001", "/book/"]

#volumes:
#  src:
